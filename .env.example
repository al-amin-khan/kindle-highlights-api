# Core
PORT=8080
NODE_ENV=development
TZ=Asia/Dhaka

# Mongo
MONGO_URI="YOUR_MONGODB_ATLAS_CONNECTION_STRING"

# JWT
JWT_ACCESS_SECRET="GENERATE_64_HEX"
JWT_REFRESH_SECRET="GENERATE_64_HEX"
ACCESS_TOKEN_TTL="15m"
REFRESH_TOKEN_TTL="30d"

# CORS
CORS_ORIGINS="*"  # keep "*" as requested (public site), can restrict later

# Public API limits
PUBLIC_DAILY_DEFAULT_LIMIT=5
PUBLIC_DAILY_MAX_LIMIT=10
PUBLIC_RATE_LIMIT_WINDOW_MS=60000
PUBLIC_RATE_LIMIT_MAX=60

# Admin bootstrap
ADMIN_BOOTSTRAP_TOKEN="set_a_one_time_secret_to_create_first_admin"

# Daily selection window mode
DAILY_WINDOW_MODE="daily"   # daily|halfday (you asked for daily now, we’ll keep option)
